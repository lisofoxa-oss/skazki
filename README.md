# Каталог сказок — GitHub Pages

## Структура

```
/index.html              ← главная страница каталога
/tales.js                ← список сказок (редактируешь только этот файл!)
/lunnyj-kot/index.html   ← шаблон страницы сказки (копируй для новых)
```

## Как добавить новую сказку

### Шаг 1 — Добавь строку в `tales.js`

Открой `tales.js` и добавь объект в массив:

```js
{
    title: "Название сказки",
    emoji: "🌙🐱",
    desc: "Краткое описание в одну-две строки",
    folder: "nazvanie-papki"
}
```

### Шаг 2 — Создай папку и скопируй шаблон

1. Создай папку с именем из поля `folder`
2. Скопируй `lunnyj-kot/index.html` в новую папку
3. Отредактируй в скопированном файле:
   - `<title>` — название сказки
   - Эмодзи в `<span class="tale-emoji-large">`
   - `<h1>` — название
   - Содержимое `<article class="tale-body">` — текст сказки

### Форматирование текста сказки

```html
<!-- Обычный абзац -->
<p>Текст абзаца...</p>

<!-- Разделитель между сценами -->
<p class="scene-break">✦ ✦ ✦</p>

<!-- Цитата или выделенный текст -->
<blockquote>Текст цитаты...</blockquote>
```

Первая буква первого абзаца автоматически станет красивой буквицей.

### Шаг 3 — Пуш в GitHub

```bash
git add .
git commit -m "Добавлена сказка: Название"
git push
```

Готово! Сказка появится на главной.

## GitHub Pages

1. Создай репозиторий на GitHub
2. Settings → Pages → Source: `main` / `root`
3. Сайт будет доступен по адресу `https://username.github.io/repo-name/`
